#include <stdio.h>
#include <limit.h>
int main(){
    int n;
    scanf("%d",&a);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<n;i++){
        if (arr[i+1]-arr[i]==)
    }
}